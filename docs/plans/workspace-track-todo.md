# 工作区开发线待办

> 负责人：工作区负责人
> 推荐分支：`main`
> 当前状态：进行中（仅推送功能分支，不推送 `main`）
> 当前任务：WS-210 接入真实视频生成、媒体产物闭环及工作区/对话区实时同步回归
> 最近更新：2026-08-21
> 最新功能提交：`d3a27ce`（WS-210 工作区/对话区实时同步）；`90119b5`（WS-210 Ark 真实视频纵切）；`8e7a515`（WS-210 私有媒体存储）；`18a5313`（WS-309 验收）；`a690890`（WS-309）
> 关联 PR：不适用（直接提交 `main`）

本文是工作区开发线的实时任务账本。产品边界以 [`docs/workspace-v2-product-spec.md`](../workspace-v2-product-spec.md) 为准；本文只记录实施顺序、责任、依赖和进度。

## 状态规则

| 状态 | 含义 |
|---|---|
| `待开始` | 依赖未满足或尚未领取 |
| `进行中` | 负责人当前正在处理；同一负责人原则上只保留一项 |
| `阻塞` | 无法继续，必须填写阻塞原因和解除条件 |
| `待评审` | 本地实现和检查已完成，等待复核或推送 `main` |
| `已完成` | 提交已进入 `origin/main`，检查和验收结果已记录 |

## 实时维护要求

1. 开始工作前先同步 `main` 与 `origin/main`，再更新页首的“当前任务、当前状态、最近更新、最新提交”。
2. 将领取任务的状态改为 `进行中`，集成目标固定为 `main`。
3. 相关检查通过后创建任务范围清晰的提交，以非强制快进方式推送 `origin/main`，并在任务备注中记录功能提交；只有用户明确要求时才创建功能分支或 PR。
4. 发生阻塞时当天改为 `阻塞`，写清依赖人、所需决定和解除条件。
5. 只有提交已进入 `origin/main`、相关检查通过并完成该任务适用的自动化或浏览器验收后才能标记 `已完成`。
6. 修改共享 Schema、工作流状态或 API 契约时，同时通知对话区负责人并更新两份清单的依赖项。
7. 每个工作日结束前刷新一次本页状态；没有进展也要确认阻塞说明仍然有效。

## 代码所有权

工作区负责人主要维护：

- `packages/schemas` 中品牌、车型、项目、任务、资产、版本和命令契约；
- `packages/domain` 中工作流、权限、revision、负责人、并发和预算规则；
- `packages/tools` 中资产、视频和自动剪辑领域端口及 Mock 适配器；
- `apps/api` 中工作区、管理中心、项目、任务和资产 API；
- `apps/web` 的应用壳、项目库、管理中心、创建向导和中间工作区。

共享热点：

- `packages/schemas/src/index.ts`：由工作区负责人维护最终结构；对话区通过评审提出字段需求。
- `apps/api/src/server.ts`：优先拆分路由后再并行修改，避免双方长期编辑同一文件。
- `apps/web/public/app.js`、`app.css`、`index.html`：先完成模块边界拆分；拆分前由工作区负责人协调合并顺序。
- `AGENTS.md` 和产品规格：任何改变业务边界的提交都需要双方评审。

## 里程碑 0：共享基座

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-001 | 盘点现有 `Project`、`Work`、车型快照和本地存储结构 | 无 | 已完成 | 形成迁移映射，列出兼容字段和废弃字段 |
| WS-002 | 定义 `Brand`、`Vehicle`、`BatchProject`、`VideoTask` Schema | WS-001 | 已完成 | TypeBox Schema、类型导出和 Schema 测试通过 |
| WS-003 | 定义资产引用、任务资产快照和临时资产 Schema | WS-002 | 已完成 | 覆盖车型、人物、场景、风格和本地上传元数据 |
| WS-004 | 定义阶段产物、不可变版本、回退和失效 Schema | WS-002 | 已完成 | 能表达版本依赖与下游失效原因 |
| WS-005 | 与对话区共同冻结 `TaskContext` 和 `AgentActionCard` 契约 | WS-002, WS-003 | 已完成 | 双方测试 fixture 使用同一版本 Schema |
| WS-006 | 将 Web 和 API 单体文件拆出工作区/Agent 模块边界 | WS-005 | 已完成 | 两条线可修改不同文件，现有功能无回归 |

## 里程碑 1：领域状态与策略

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-101 | 将工作流改为首版“策略 → 资产匹配 → 脚本 → 分镜 → 视频预览 → 交付”（已由 WS-108 取代） | WS-004 | 已完成 | 合法/非法转换均有测试 |
| WS-102 | 实现每阶段人工确认和不可变版本落盘 | WS-101 | 已完成 | Agent 无法自批，人工事件可审计 |
| WS-103 | 实现历史版本回退及下游产物失效 | WS-102 | 已完成 | 回退后的依赖产物不能继续使用 |
| WS-104 | 实现品牌、项目访问和任务负责人权限 | WS-002 | 已完成 | 跨租户、跨品牌、非负责人操作被拒绝 |
| WS-105 | 实现任务接管和乐观锁 | WS-104 | 已完成 | 同一时刻只有一个负责人，旧 revision 被拒绝 |
| WS-106 | 实现账号级高消耗任务运行锁 | WS-104 | 已完成 | 同账号第二条任务被拒绝，不同账号可分别运行 |
| WS-107 | 实现账号额度、成本预估和扣减规则 | WS-104 | 已完成 | 额度不足由后端拒绝，失败任务不错误扣费 |
| WS-108 | 将工作流调整为“策略 → 脚本 → 资产匹配 → 分镜 → 视频预览 → 交付” | WS-103, WS-203 | 已完成 | 车型在策略开始时锁定，素材在匹配确认时锁定；转换、版本依赖、回退失效、迁移兼容和 Agent 上下文全部使用新顺序 |

## 里程碑 2：领域端口和模拟适配器

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-201 | 定义公司资产库 Provider 接口 | WS-003 | 已完成 | UI 和状态机不依赖 Mock 私有结构 |
| WS-202 | 实现可筛选的模拟公司资产目录 | WS-201 | 已完成 | 支持车型、人物、场景、品牌风格查询 |
| WS-203 | 实现项目资产池与任务资产快照 | WS-201, WS-102 | 已完成 | 项目看最新资产，任务锁定版本 |
| WS-204 | 实现本地临时资产元数据、校验和版权声明流程 | WS-203 | 已完成 | 格式、尺寸、重复项和来源校验可演示 |
| WS-205 | 预留 Seedance 2.5 视频生成 Provider 接口 | WS-107 | 已完成 | 冻结服务端作用域、提交/查询/取消和公共任务 DTO；不实现适配器 |
| WS-206 | 预留自动剪辑 Provider 接口 | WS-205 | 已完成 | 冻结供应商无关的提交/查询/取消和草稿 DTO；不实现适配器 |
| WS-207 | 导入零跑 C10 公司素材目录并接入项目资产池 | WS-202, WS-203, WS-308 | 已完成 | 功能提交 `558077e` 已进入 `origin/main`；55 张素材具有稳定描述与车型作用域，已幂等写入 PostgreSQL 中独立的 C10 项目资产池；受鉴权媒体接口和前端 Blob 缩略图链路在真实页面显示 55/55，且不跨车型混用 |
| WS-210 | 接入真实视频生成、异步作业与媒体产物闭环 | WS-107, WS-205, WS-207, WS-309, WS-408 | 进行中 | 已实现显式人工提交、Ark 异步任务轮询、超时取消、终态额度结算/运行锁释放、真实 MP4 校验、私有对象存储、`media_artifacts` 与网页认证播放；默认关闭且自动化测试不产生费用。最终完成仍需持久化作业调度与重启恢复 |

## 里程碑 3：API 和数据迁移

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-301 | 实现开发账号切换和服务端 SessionScope | WS-104 | 已完成 | 管理员和多个制作账号可切换，权限不由客户端伪造 |
| WS-302 | 实现品牌、车型、授权和额度管理 API | WS-301, WS-202 | 已完成 | 管理中心所需查询和命令完整 |
| WS-303 | 实现三步创建项目 API | WS-203, WS-301 | 已完成 | 车型、资产包、风格/画幅均由服务端校验 |
| WS-304 | 实现视频任务创建、分配、接管和列表 API | WS-105, WS-303 | 已完成 | 项目可包含多条独立任务 |
| WS-305 | 实现统一命令 API 执行 `AgentActionCard` | WS-005, WS-107 | 已完成 | 校验任务、负责人、revision、状态和成本 |
| WS-306 | 实现阶段确认、版本查询、回退和失效 API | WS-103, WS-304 | 已完成 | 全部操作可审计且幂等 |
| WS-307 | 迁移现有作品到“示例品牌 → 车型项目 → 视频任务” | WS-002, WS-304 | 阻塞 | 策略、状态、版本、锁定和审批记录保留 |
| WS-308 | 确认并接入 PostgreSQL 持久化与迁移基座 | WS-304 | 已完成 | PostgreSQL 配置、迁移、readiness 与七类权威 Store 已保留；适配器一致性、幂等性和连接安全加固已通过最新主线整合与全仓验证 |
| WS-309 | 规范化车型系列、销售版本、事实修订和任务车型快照 | WS-308 | 待评审 | PostgreSQL 分层保存车型、可选销售版本、不可变字符串事实修订与任务锁定快照；参数键值不作为车型事实来源；本轮仅提交当前本地分支，不推送 `main` |

## 里程碑 4：前端工作区

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-401 | 建立桌面端应用壳、品牌导航和账号切换器 | WS-006, WS-301 | 已完成 | 1280–1920px 正常使用，小屏有明确提示 |
| WS-402 | 实现项目库首页和“我的进行中任务” | WS-304, WS-401 | 已完成 | 支持搜索、品牌/车型筛选和最近活动排序 |
| WS-403 | 实现单页新建项目 | WS-303, WS-304, WS-401 | 已完成 | 选择品牌/车型、10/15/30 秒时长和三种广告类型，并自动创建首条 Agent 任务 |
| WS-404 | 实现三栏项目工作区框架 | WS-006, WS-304 | 已完成 | 顶部模块、左栏、中间区和 Agent 插槽稳定 |
| WS-405 | 实现项目概览、任务列表、负责人和状态展示 | WS-404 | 待评审 | 切换任务同步 URL、状态和上下文事件 |
| WS-406 | 实现资产匹配与临时资产上传界面 | WS-204, WS-404, WS-108, AG-307 | 待评审 | 策略和脚本确认后，Agent 按资产描述选取素材；车型资产锁定，人物/场景可人工替换 |
| WS-407 | 实现策略、脚本、分镜、预览和交付模块 | WS-306, WS-404, WS-108 | 已完成 | 分镜逐镜展示引用素材并支持人工调整；每阶段支持确认、历史版本和回退提示 |
| WS-408 | 实现成本预估、额度和运行锁反馈 | WS-106, WS-107, WS-404 | 已完成 | 前端提示与后端拒绝原因一致 |
| WS-409 | 实现管理中心 | WS-302, WS-401 | 已完成 | 品牌、车型、资产关联、账号授权和额度可管理 |

## 里程碑 5：集成与验收

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| WS-501 | 与对话区联调任务切换和 `TaskContext` | WS-404, AG-301 | 待评审 | 不发生跨任务上下文泄漏 |
| WS-502 | 联调 Agent 操作卡片统一命令 API | WS-305, AG-401 | 已完成 | 旧 revision、越权和超额卡片均被拒绝 |
| WS-503 | 完整走通项目创建到模拟交付 | WS-407, WS-502 | 待开始 | 产品规格中的 12 条验收标准逐项记录结果 |
| WS-504 | 运行全仓检查和桌面端浏览器视觉验收 | WS-503 | 待开始 | `npm run check` 通过，无真实供应商费用 |
| WS-505 | 更新架构文档、迁移说明和发布说明 | WS-504 | 待开始 | 文档反映最终实现而非计划状态 |

## 当前阻塞

- WS-307（责任人：工作区业务负责人、迁移执行负责人）：迁移代码、离线工具和只读预演已完成，但当前协调器仍以 Local V2 Store 为写入目标，不能把旧 `.data` 导入 PostgreSQL 权威 Store，真实 `apply` 尚不可执行。解除阻塞前必须先新增/注入 PostgreSQL target adapter，并确定停服窗口，冻结画幅、owner/creator/审计账号及展示名、受众/主题/时长/平台标签、项目/素材 ID、V3 Session 映射；随后针对 PostgreSQL 目标重新 dry-run，审批最终数量、冲突报告和 plan hash。当前开发期 hash 不得用于正式迁移。
- WS-308（责任人：工作区/数据库整合负责人）：用户已撤销此前的回滚决定，当前主线继续保留 PostgreSQL 配置、迁移、readiness、持久化适配器和集成测试。未配置真实测试数据库地址时集成用例按安全门跳过，生产配置后使用 PostgreSQL 权威 Store；V2 `TaskContext` 始终从当前任务权威 Store 解析，不得回退 legacy `.data/works`。整合提交 `5b50f10` 与加固提交 `1e9f511` 已覆盖七类权威 Store、任务转换审计、项目创建并发幂等、连接/事务安全、管理事实权威性和 readiness 漂移校验，本项关闭。

## 任务备注

- WS-001：已进入 `origin/main`；提交 `b589528`；迁移映射、兼容字段和废弃字段验收记录完整。
- WS-002：已进入 `origin/main`；提交 `5b21d7e`；四个 Workspace V2 核心实体、类型导出和边界测试验收通过。
- WS-003：已进入 `origin/main`；提交 `e664b30`；资产引用、资产池、任务快照和临时资产元数据验收通过。
- WS-004：功能提交 `86414a1` 随本次账本提交进入 `origin/main`；阶段内容引用、不可变版本、人工确认、回退记录和依赖失效契约验收通过，`npm run check` 通过（58/58 测试）。
- WS-005：功能提交 `a8e13b2`；冻结服务端解析的只读 `TaskContext`、统一提案型 `AgentActionCard`、成本提示与三类版本化 payload；旧 `StrategyActionProposal` 保留为已弃用兼容契约。工作区 Schema 测试和对话区工具测试共享同一 fixture，全仓检查通过（62/62 测试）。
- WS-006：功能提交 `005a41e`；API 入口缩减为组合层，工作区命令、Agent 会话/SSE、HTTP 基础设施和静态资源分别维护；Web 使用 ES 模块拆分工作区 API/壳与 Agent API/面板/样式。全仓检查通过（62/62 测试），本地浏览器验证模块加载、阶段切换、Agent 快捷提示及零控制台错误。
- WS-101：功能提交 `44b1cf0`；冻结 `VideoTaskStageStatus` 和首版六阶段顺序，V2 状态机只允许当前阶段提交确认、人工驳回或人工确认，禁止跳阶段、Agent 自确认和终态继续推进；旧 `WorkStatus` 明确降级为兼容链路。该阶段顺序已由 WS-108 取代。全仓检查通过（66/66 测试）。
- WS-102：功能提交 `7e8d318`；人工确认命令不接收客户端身份或来源，服务端会话注入租户、项目和负责人作用域；确认事件与不可变阶段版本互相引用，并随任务 revision 和阶段推进作为单一聚合原子落盘。覆盖六阶段、版本递增、防御性副本、旧 revision 重复确认、越权及保存失败无部分写入，全仓检查通过（74/74 测试）。
- WS-103：功能提交 `fb3f303`；聚合记录各阶段当前选中版本、回退审计和失效事件，负责人回退后按 `stage_artifact` 依赖图递归失效全部下游版本、清除受影响的活动指针并重置工作流；新确认禁止引用已失效或非当前上游版本。兼容升级 WS-102 schema v1 落盘记录，回退、失效和 revision 原子保存；全仓检查通过（85/85 测试）。
- WS-104：功能提交 `80f67f7`；新增可持久化的品牌级与车型项目级 `WorkspaceAccessGrant`，以及服务端 `WorkspaceSessionScope` 下的品牌管理、项目创建/查看、任务查看/操作/接管权限规则。确认和回退运行时先验证真实批次项目、有效授权及当前负责人；跨租户、跨品牌、撤销/他人授权、无项目资格和非负责人写入均被拒绝，全仓检查通过（93/93 测试）。
- WS-105：功能提交 `2eefee7`；接管请求只接受预期 revision 和人工原因，新负责人从服务端会话解析，所有任务聚合写入按任务串行并在事务内复核 revision；接管会原子更新唯一负责人、revision 和不可变审计记录，并兼容升级既有 v1/v2 落盘数据。全仓检查通过（100/100 测试）。
- WS-106：功能提交 `5c48702`；新增租户账号级高消耗任务运行锁，锁定视频生成和自动剪辑操作，身份仅取自服务端会话；同账号并发竞争仅一个成功，不同账号可分别运行，释放需匹配锁 ID，持久化锁可跨服务重启恢复。HTTP 冲突稳定返回 `AIC-CONCURRENCY-ACCOUNT_HIGH_COST_TASK_RUNNING`，全仓检查通过（113/113 测试）。
- WS-107：功能提交 `d391839`；新增账号独立额度、版本化成本预估和整数最小货币单位账本。高消耗操作执行时由后端重新估价并原子预留额度，成功后只扣实际成本并释放差额，失败则完整释放且零扣费；并发预留不能超额，额度与运行锁可跨服务重启恢复。全仓检查通过（132/132 测试）。
- WS-108：权威顺序已调整为 `strategy → script → asset_matching → storyboard → video_preview → delivery`。策略开始只锁定车型事实；资产匹配由负责人明确确认时，在同一任务事务内原子建立最终素材快照、阶段产物、人工确认和 receipt，revision 只递增一次。策略/脚本不依赖素材快照，资产匹配及后续产物依赖素材快照与直接上游；回退按显式依赖边递归失效并兼容旧顺序历史。任务聚合升级为 schema v7 并显式升级 v1–v6；V1 Work 状态、TaskContext 和 Agent 建议上下文均已投影到新顺序。重放 `origin/main@b56014d` 后最终全仓检查为 545 项（544 通过、1 项因未配置 `TEST_DATABASE_URL` 安全跳过），类型检查、凭据扫描与 diff 检查通过，三路独立复核无 P0/P1/P2。加固提交 `977b4f1` 已非强制快进进入并核验存在于 `origin/main`，本项已完成。
- WS-201：功能提交 `8f40dff`；仅预留只读 `CompanyAssetProvider` 端口，冻结服务端授权作用域、车型/品牌/分类/关键词分页查询、共享资产引用批量解析和公共展示 DTO。未实现生产 Mock、模拟目录或筛选适配器，后续 WS-202 必须在该端口内实现。全仓检查通过（134/134 测试）。
- WS-202：功能提交 `5aa5d7c`；在 `CompanyAssetProvider` 端口内实现只读模拟目录，覆盖车型、人物、场景和品牌视觉风格，支持授权范围、分类、品牌、车型、关键词、标签及稳定游标分页过滤。目录只返回最新版本，历史版本仍可按共享引用精确解析；跨租户/品牌/车型数据和 Mock 私有排序字段不会泄漏。全仓检查通过（143/143 测试）。
- WS-203：功能提交 `031cee0`；项目资产池按项目独立持久化并在读取时刷新同一公司资产身份的最新版本，首版实现曾在任务进入策略阶段时原子写入素材快照。WS-108 已将当前边界调整为策略开始只锁定车型事实、资产匹配确认时才原子锁定最终素材快照和指针；所选目录版本仍须精确解析，车型资产不能跨车型或替换身份。WS-203 当时完成了 v1/v2/v3 → schema v4 兼容升级和 164/164 测试。
- WS-204：功能提交 `ef3145c`；新增项目级临时资产元数据 Store、服务端可信文件检查输入和来源/版权声明流程。JPEG/PNG/WebP/MP4/WebM 的格式扩展名、大小、尺寸、SHA-256 重复项和有效期由服务端规则校验；技术失败进入 `rejected`，重复或版权未确认进入 `needs_review`，只有无问题的 `valid` 资产能加入项目资产池。项目级协调锁串行化临时资产、资产池和任务快照操作；任务锁定前会重新核验本地引用，过期或被篡改的素材不能进入快照。全仓检查通过（189/189 测试）。
- WS-205：功能提交 `3b663bb`；仅预留目标模型固定为 `seedance-2.5` 的视频生成 Provider 端口，包含服务端生产作用域、幂等提交、状态查询、取消、失败信息和内部视频产物句柄。请求 DTO 不携带租户/账号权限、预算、审批、运行锁或供应商下载地址；未实现 Mock 或真实适配器。
- WS-206：功能提交 `04cbe51`；仅预留供应商无关的自动剪辑 Provider 端口，包含幂等提交、状态查询、取消、源视频引用和内部草稿句柄。未加入剪映或未来自动化工具私有字段，未实现 Mock 或真实适配器。两项合并后的全仓检查通过（193/193 测试）。
- WS-301：功能提交 `7c27c81`；新增 1 个内容管理员和 2 个制作账号的服务端开发目录、8 小时 opaque Bearer Session、摘要化原子持久化和请求级 `WorkspaceSessionScope` 动态装配。客户端只能选择 `accountId`，租户、角色和授权均由服务端目录/授权 Provider 解析；切换账号吊销旧 token，退出/过期/未知 token 稳定拒绝。认证 Agent 会话复用 AG-103 的 v3 授权门，持久绑定账号、租户、项目和任务；越权或不存在会话使用相同认证拒绝，匿名旧作品/Agent 仅在本机非生产兼容模式开放。全仓检查通过（228/228 测试）。
- WS-302：功能提交 `1b0efd2`；新增租户级原子持久化的品牌、不可变车型事实版本、车型推荐资产关联和账号授权目录，并与 Session 共用同一授权源。管理 API 覆盖品牌/车型/事实历史、模拟公司资产筛选与精确关联、账号授权、额度配置、本人额度和消费概览；品牌、授权、资产包与预算命令使用 revision/version 乐观锁，目标账号、归档状态、跨品牌/车型资产和 Provider 错配均由服务端拒绝。新建车型可获得确定性的车型 Mock 资产，品牌默认视觉预设必须解析为可见的公司视觉风格。全仓检查通过（237/237 测试）。
- WS-304：功能提交 `40f8750`；新增视频任务创建、显式负责人分配、当前会话账号接管和项目任务列表 API。创建按 tenant/project/actor/requestId 幂等，初始状态固定为 strategy/in_progress/revision 1；分配和接管原子更新唯一 owner、revision 与不可变审计。持久化 Store 通过跨实例目录锁、lease 心跳和 token write fence 防止双成功、旧事务覆盖及审计丢失；动态撤权、跨租户/项目、非负责人、旧 revision 和失锁写入均被拒绝。最终复审无 P0/P1/P2，全仓检查通过（302/302 测试）。
- WS-305：功能提交 `3bf2a01`；新增认证工作区统一命令端点 `POST /v1/workspace/batch-projects/:projectId/video-tasks/:taskId/commands`，请求严格限制为 `{ requestId, card }`。服务端从 Bearer Session 和最新授权快照重新校验租户、项目、任务、制作角色、当前负责人、revision 与工作流状态，不信任卡片展示文案或成本。命令按 task/actor/requestId 幂等；经 WS-108 调整后，策略生成在单次任务事务内只锁定项目冻结的车型事实版本，并原子写入策略草稿与 receipt，最终素材快照延后到资产匹配确认。提交审批只进入等待人工确认，阶段回退复用领域回退规则并原子记录失效和 receipt。三类当前命令的服务端权威成本均为 `free / 0 / charged=false`。WS-305 当时把任务聚合升级为 schema v5 并通过 332/332 测试；当前聚合版本见 WS-108。
- WS-306：功能提交 `9271e1c`；新增阶段版本、全任务失效审计、显式人工确认和阶段回退四个认证 API。服务端重新校验 SessionScope、动态授权、项目/任务作用域、当前负责人和 revision，按 actor/requestId 在统一命令与阶段写入间保证幂等互斥；确认依赖和策略产物哈希由服务端推导，回退精确绑定 before→after 并重算完整递归失效闭包。WS-306 当时把任务聚合升级为 schema v6、兼容 v1–v5；WS-108 已在不丢失历史、receipt 或审计图的前提下继续升级为 schema v7。WS-306 最终复审无 P0/P1/P2，全仓检查通过（366/366 测试、类型检查、凭据扫描）。
- WS-307：功能提交 `064f74a`；实现 V1 Work/Session 到 V2 项目、任务、快照、策略、审批及 task-scoped v3 Session 的确定性迁移，并提供 `plan`、`apply`、`resume`、`status`、`restore` 离线 CLI、源数据备份、manifest、plan hash、进程租约、断点续跑和安全恢复。迁移完成后 V1 GET 保留、V1 写入返回 410，Agent 改用动态 V2 `TaskContext`、实时状态和锁定车型快照，Web 一次性迁移 `firefly.workId`。变基保留 WS-402 项目库和 AG-404 错误分类后独立复核无 P0/P1/P2，全仓检查通过（415/415 测试、类型检查、凭据扫描）。2026-08-19 最新开发期只读预演为 87 个 Work、35 个 Session（21 个绑定任务）、124 个目标写入，plan hash `b293c508484d46c12c60047703c96957446bcc15d1dd2959b1cd3eeb6174c190`；预演前后 manifest 均为 `null`，从未执行真实 `apply`。源目录仍可增长，该 hash 仅作开发证据，不得用于正式迁移。
- WS-308：加固提交 `1e9f511` 修复 PostgreSQL VideoTask 转换不变量旁路、并发创建项目幂等重放、连接串参数覆盖、未知事务连接回池、管理事实代码 seed 回退和专项测试空跑；readiness 现精确校验关键列及 41 个 PostgreSQL canonical CHECK 定义，真实回归覆盖 `OR TRUE`、括号重分组和 JSON 键大小写漂移。数据库 Store、迁移和 readiness 仍仅为未启用适配器，不代表当前产品已完成接入；Agent、领域工具和提示词不得获得 SQL、连接串、数据库工具或通用文件系统工具。
- WS-108：功能提交 `b98564e` 明确实现“策略 → 脚本 → 资产匹配 → 分镜 → 视频预览 → 交付”；加固提交 `977b4f1` 将资产确认统一为 canonical 阶段确认事务，补齐精确版本校验、旧顺序回退、v1–v6 → v7 升级和上下文同步边界。策略开始只锁车型事实，资产匹配确认才按人工最终选择锁定精确素材版本；确认依赖、回退失效和 Agent 阶段上下文均使用新顺序。
- WS-406：功能提交 `b98564e` 提供全中文资产匹配页、车型锁定、人物/场景人工替换、项目级临时图片上传与服务端文件内容校验。策略和脚本未确认时不提前推荐；人工选择不会被推荐刷新静默覆盖。全仓 `npm run check` 通过（517 通过、1 个真实 PostgreSQL 用例按安全门跳过）；浏览器验收覆盖 1920px、1279px 桌面提示、明暗主题、零横向溢出、零控制台错误和无效图片中文反馈。WS-407 未开始。
- WS-407：功能提交 `ba84084` 实现全中文策略、脚本、逐镜分镜、模拟视频预览和交付模块，并接入 WS-306 的真实阶段版本查询、人工确认与回退 API。历史版本明确区分当前、历史和失效状态，回退前列出全部下游失效阶段；非策略阶段只使用服务端返回的持久化产物引用，不伪造产物 ID 或内容哈希。分镜按时长生成镜头清单，逐镜展示锁定车型及人物/场景素材，车型不可替换，人物/场景人工调整优先并提示写入新分镜版本。真实视频和剪映 Provider 尚未接入，页面明确标为模拟预览与等待生成。全仓 `npm run check` 通过（521 通过、1 个真实 PostgreSQL 用例按安全门跳过）；浏览器验收覆盖 1920px、1280px、1279px、明暗主题、零横向溢出和零控制台错误。WS-408 未开始。
- WS-408：功能提交 `f6fe970` 及账号异步隔离修复 `0125c06` 已进入并核验存在于 `origin/main`。制作与交付模块加入预估费用、本人可用额度和账号运行槽反馈；额度响应严格校验账号、币种及总额恒等式，运行状态区分可用、本任务运行中和其他任务占用。新增认证只读接口仅按 Session 返回当前账号的公开锁信息，不暴露租户、账号或锁标识；额度不足、未配置额度、运行锁冲突和 revision 冲突使用后端稳定代码对应的中文提示。真实视频、剪映和定价 Provider 尚未接入，不伪造报价。修复后全仓 556 项通过、1 个真实 PostgreSQL 用例按安全门跳过；类型检查、脚本语法、凭据扫描、diff 检查和浏览器验收通过，本项已完成。
- WS-409：功能提交 `f236ee0` 已进入并核验存在于 `origin/main`。管理中心覆盖概览、品牌创建/编辑/归档恢复、车型及不可变事实版本、公司资产浏览与车型资产关联、账号授权撤销恢复、额度创建调整和多币种最小单位显示；身份、租户、权限、revision、品牌风格作用域及至少一项当前车型资产均由服务端复核。重放 WS-408、AG-307、AG-504 后全仓 `npm run check` 通过（575 通过、1 个真实 PostgreSQL 用例按安全门跳过），类型检查、凭据扫描、脚本语法和 diff 检查通过；浏览器验收覆盖直接深链、Back/Forward、焦点恢复、1280–1920px、1279px 提示、明暗主题及零控制台错误。未调用真实模型、未产生供应商费用、未写生产数据库，本项已完成。
- WS-401：功能提交 `83cbbad`；完成可嵌入萤火虫平台的桌面工作区壳，页面直接从项目列表开始，品牌切换置于顶部，并保留账号、主题和服务状态入口；界面文案统一为中文，品牌、项目及智能助手上下文按账号隔离。全仓检查通过（338/338 测试、类型检查、凭据扫描）；浏览器完成 1280×900、1920×1080 与 1279×900 边界验收，明暗主题、助手折叠和多品牌切换正常，无控制台错误；用户已确认并同意进入下一项。
- WS-402：功能提交 `1f8e035`；新增认证项目库聚合 API 和全宽项目库首页，展示当前账号可见项目及“我的进行中任务”，支持顶部品牌、车型、项目状态、中文搜索和最近活动/项目名称排序。服务端按最新授权快照过滤项目，车型使用冻结事实版本，最近活动统一按真实时间排序，响应不暴露负责人身份和生产聚合内部数据；前端覆盖加载、空数据、无结果、错误重试、品牌不可用及账号切换竞态。全仓检查通过（371/371 测试、类型检查、凭据扫描）；浏览器完成 1280×900、1920×1080、1279×900、明暗主题、组合筛选、账号切换和错误恢复验收，全新会话控制台 0 错误、0 警告，等待用户评审。未开始 WS-403。

- WS-501：功能提交在当前 WS-409 基线上重放为 `c6e5a9e`，验收记录为 `d511eb4`；复用 AG-504 已落地的 V2 Agent 面板与按任务恢复会话能力，并补充工作区侧协议漂移保护。Session、`TaskContext` 与当前任务的项目、品牌、车型事实版本必须完全一致；流式事件和完成结果必须匹配当前任务；账号、任务、会话或 scope generation 变化后旧异步结果一律丢弃。运行中“资产池”和浏览器历史导航不能绕过任务切换锁。全仓检查通过（563 项：562 通过、1 项因未配置真实 PostgreSQL 安全跳过），类型检查和凭据扫描通过；浏览器验证两个任务独立恢复消息与会话、账号切换清空上下文、1280/1279 边界、暗色主题、零横向溢出及零控制台错误/警告。用户已验收，等待随 WS-502 提交链进入主线。

- WS-502：功能提交 `74ad611`；操作卡片使用 `sessionId + toolCallId/eventId + 权威业务字段` 的 SHA-256 稳定请求标识，任务会话重建后仍能复用同一幂等请求。命令成功、失败和忙碌状态回写均校验当前账号、会话、项目、任务及 scope generation，旧异步结果不能污染新上下文或清除新操作状态。HTTP 集成测试覆盖旧 revision 409、其他负责人 403，以及注入 `AIC-COST-BUDGET_EXCEEDED` 时 409 且 `charged=false`；前端对应显示“已失效”“无执行权限”“额度不足”并阻止旧卡片重提。当前统一命令动作仍由服务端权威重估为免费，不按卡片展示金额误拒绝；真实高消耗动作继续使用 WS-408 的预算与运行锁链路。原功能验收覆盖 1280px 明暗主题、1279px 桌面提示、入口缓存版本、零横向溢出和零控制台 warning/error；2026-08-21 最终评审未发现 P0/P1/P2，聚焦测试 24/24、当前全仓门禁 600 项中 599 通过且 1 项真实 PostgreSQL 用例按未配置 `TEST_DATABASE_URL` 的安全门跳过，类型检查与凭据扫描通过。用户确认关闭，本项已完成；WS-503 未开始。

## 更新记录

| 日期 | 更新人 | 说明 |
|---|---|---|
| 2026-08-21 | Codex | 修复统一命令完成后工作区仍需整页刷新的同步回归，功能提交 `d3a27ce`：阶段面板和资产匹配面板不再只比较项目/任务 ID，而会把任务 revision、当前阶段、阶段状态和任务状态纳入上下文键；同一任务被 Agent 命令更新后会自动失效旧视图并只重新拉取当前面板。全仓 668 项中 667 通过、1 项真实 PostgreSQL 用例按未配置 `TEST_DATABASE_URL` 的安全门跳过、0 失败；类型检查、凭据扫描通过，目标项目 Browser 深链加载正常且零 warning/error。仅推送 `codex/db-assets-connect`。 |
| 2026-08-21 | Codex | 用户明确要求把当前模拟制作链路改为真实视频生成并取得真实产物；代码与配置只提交并推送 `codex/db-assets-connect`，不进入 `main`。领取 WS-210：先完成供应商、异步作业、预算/运行锁终态、私有媒体存储和真实播放器代码及无付费测试，再在真实调用前向用户确认 API Key、模型和费用上限。已刷新 `origin/main`；根工作区 `main` 存在大量既有未提交改动，保持不动，不执行破坏性同步。 |
| 2026-08-21 | Codex | WS-210 首个真实纵切已完成代码接线：运行时 Ark Model ID、严格供应商适配器、服务端报价、显式人工提交、轮询/超时取消、锁与额度终态结算、MP4 字节元数据/哈希校验、MinIO/S3 私有入库、认证短期播放和刷新恢复待确认产物。付费调用仍关闭；跨进程重启恢复调度器尚未完成，因此 WS-210 保持进行中。 |
| 2026-08-21 | Codex | WS-210 代码门禁通过：全仓 666 项中 665 通过、1 项因未配置独立 `TEST_DATABASE_URL` 按设计跳过、0 失败，TypeScript、凭据扫描、diff 检查和 Docker Compose 配置校验通过；提交为 `8e7a515` 与 `90119b5`。等待仅推送 `codex/db-assets-connect` 后，向用户确认 Ark 视频 API Key、实际 Model/Endpoint ID 和单次费用上限，再启动本地 MinIO/API 并执行真实付费验收。 |
| 2026-08-21 | Codex | 复用并审计 WS-207 未合并的私有对象存储实现：视频字节写入私有 S3-compatible bucket，PostgreSQL 仅保存作用域、对象键、哈希和媒体元数据，认证 API 动态签发短期 URL；不会把 URL、凭据或视频二进制写入业务聚合。基础实现全仓检查 587 通过、1 项真实 PostgreSQL 测试按配置跳过、0 失败。 |
| 2026-08-21 | Codex | WS-309 本地功能提交 `a690890`：迁移 3 新增车型系列、销售版本、不可变字符串事实修订与任务车型快照；`factsText` 已贯通管理目录、任务锁定快照和 Agent 只读领域工具，校验索引与正文哈希不一致时失败关闭。真实 PostgreSQL 已迁移并验证 C10 纯电 v3/增程 v2，V3 项目导入 55 项素材，策略命令锁定的快照与事实修订哈希一致，事实 UPDATE 被不可变触发器拒绝。全仓 614 通过、1 项真实 PostgreSQL 测试因未配置独立 `TEST_DATABASE_URL` 按门禁跳过、0 失败，凭据扫描通过。按用户要求仅提交 `codex/db-assets-connect` 本地分支，不推送任何远端或 `main`。 |
| 2026-08-21 | Codex | WS-207 功能提交 `558077e` 已非强制快进进入并核验存在于 `origin/main`。迁移版本 2 校验通过，C10 品牌、车型、授权、项目、任务及 55 张素材幂等落入 PostgreSQL；页面以制作账号 B 读取同一任务和资产池，55/55 张受鉴权 WebP 缩略图成功显示。真实 DeepSeek Agent 用一句话正确返回“零跑 C10、策略阶段”，未执行业务操作，页面与服务端零 warning/error。重放最新主线后全仓 609 通过、1 个按安全门跳过、0 失败，独立 `firefly_test` PostgreSQL 集成测试 10/10，凭据扫描通过；WS-207 标记完成。 |
| 2026-08-21 | Codex | WS-207 / WS-308 接入代码、迁移 2 兼容、C10 受鉴权媒体路由与幂等数据库初始化脚本已完成；真实 55 张文件（49,728,664 字节）逐一通过完整性校验。执行数据库写入前 Docker Desktop 一度停止，因此当时未发生部分写入；服务恢复后的最终结果见上一条。 |
| 2026-08-20 | Codex | 用户明确要求连接本机 PostgreSQL，并将上一会话整理的零跑 C10 素材导入资产池。已从 `origin/main@99424b5` 建立独立 `codex/db-assets-connect` 工作树；本轮按车型隔离补齐 C10 品牌/车型/授权/关联和独立项目资产池，不改写现有 E5 项目。 |
| 2026-08-21 | Codex | 用户评审并关闭 WS-502。最终复核确认稳定幂等 requestId、命令上下文漂移保护、旧 revision/非负责人/额度不足 HTTP 拒绝和防重复提交边界均无回退；聚焦测试 24/24、全仓 599/600 通过，1 项真实 PostgreSQL 用例因未配置 `TEST_DATABASE_URL` 按安全门跳过，类型检查与凭据扫描通过。WS-503 保持待开始。 |
| 2026-08-20 | Codex | 用户撤销回滚后，PostgreSQL 配置、迁移、readiness 与七类权威 Store 已随整合提交 `5b50f10` 保留；最终 589 项门禁、TypeScript 和凭据扫描通过，WS-308 标记完成。 |
| 2026-08-20 | Codex | WS-308 / AG-504 功能提交 `1e9f511` 与验收台账提交 `488769d` 已进入并核验存在于远端主线；自动检查、真实 PostgreSQL、Browser 和独立复审证据齐全。用户已撤销回滚，PostgreSQL 实现继续作为正式持久化方案保留。 |
| 2026-08-20 | Codex | WS-308 加固提交 `1e9f511` 修复任务转换审计、项目创建并发幂等、连接/事务安全、管理事实权威性和 readiness 漂移误判；真实 PostgreSQL 10/10、全仓 595 通过/1 按设计跳过、凭据扫描和本地页面验收通过，独立复审无 P0/P1/P2。 |
| 2026-08-20 | Codex | WS-502 功能提交 `74ad611` 完成稳定幂等 requestId、命令上下文漂移保护、入口缓存刷新和旧 revision/其他负责人/预算不足 HTTP 拒绝矩阵；前端稳定中文错误状态会阻止旧卡片重提。全仓与浏览器验收通过，任务转为待评审，WS-503 保持待开始。 |
| 2026-08-20 | Codex | 用户确认 WS-409 已完成并开始 WS-502。基于 `origin/main@864564f` 建立独立 `codex/ws502` 工作树，带入已验收的 WS-501 任务上下文隔离提交；开始复核操作卡片从人工点击、统一命令、服务端重新鉴权/版本/额度校验到前端结果状态的完整闭环。 |
| 2026-08-20 | Codex | 用户要求修复已发现的数据库问题。基于已同步的 `origin/main@864564f` 重新领取 WS-308 的适配器加固范围，优先修复视频任务不可变转换校验、项目创建并发幂等及数据库连接防御，并补充聚焦测试；该阶段的临时运行边界已被后续“撤销回滚、继续 PostgreSQL”决定取代。 |
| 2026-08-20 | Codex | WS-408 功能提交 `f6fe970`、账号隔离修复 `0125c06` 与 WS-409 功能提交 `f236ee0` 已通过普通快进进入并核验存在于 `origin/main`。最终合并门禁为 575 项通过、1 项真实 PostgreSQL 集成测试按配置跳过；管理中心浏览器验收与控制台检查通过，WS-408、WS-409 标记为已完成。 |
| 2026-08-20 | Codex | WS-501 在独立 `codex/ws501` 工作树完成，与 WS-409 并行且无工作树混用。复用 AG-504 会话同步，新增 TaskContext 全绑定校验、SSE 任务校验、旧异步回写丢弃及历史导航锁；全仓检查和双任务浏览器验收通过，转为待评审，WS-502 保持待开始。 |
| 2026-08-20 | Codex | 用户撤销 WS-308 回滚决定：主线继续保留 PostgreSQL 配置、迁移、readiness、持久化适配器和测试，不再按“仅本地 Store”方案删除或降级数据库实现。当前素材测试参考目录仍为 `D:\work_zy\业务部\萤火虫平台\素材`，清洗与图片描述可在后续资产任务中拆分窗口处理。 |
| 2026-08-20 | Codex | WS-108 加固提交 `977b4f1` 已非强制快进推送，且本地 `HEAD`、`origin/main` 与远端包含性核验一致。六阶段新顺序、车型/素材分时锁定、单事务确认、schema v7 迁移兼容、依赖图回退及 Agent/TaskContext 边界均达到完成标准；WS-108 标记为已完成。 |
| 2026-08-20 | Codex | WS-108 最终门禁和独立复核完成：重放 `origin/main@b56014d` 后全仓 545 项中 544 通过，1 项真实 PostgreSQL 集成测试因未配置 `TEST_DATABASE_URL` 按安全门跳过；类型检查、凭据扫描和 diff 检查通过，三路复核无 P0/P1/P2。`DATABASE_URL` 未配置，因此本轮 `db:status` 未执行成功且不误报；现将任务转为待评审，等待非强制快进推送和远端确认。 |
| 2026-08-20 | Codex | WS-108 本地实现与分域聚焦验收已完成：六阶段新顺序、车型/素材分时锁定、不可变依赖、依赖图回退、schema v7 与 v1–v6 升级、V1 Work 迁移以及 Agent/TaskContext 上下文均已对齐。当前等待主流程全仓检查、独立复核和非强制快进推送，任务继续标记为进行中，不提前宣称完成。 |
| 2026-08-20 | Codex | 开始 WS-308 语义整合：以含 WS-307 与对话鉴权台账的基线整理 PostgreSQL 文档和台账。数据库本地验收已覆盖显式迁移、readiness 与真实 PostgreSQL 测试；仍待最新代码整合、全仓检查、页面链路验收、非强制快进推送和 CI，任务保持进行中。 |
| 2026-08-20 | Codex | WS-308 已基于经逐文件核对的 `origin/main@7fc45d6` 完成语义整合：保留 WS-307 V2 TaskContext、WS-403/404 与最新工作流决策，并纳入对话 Bearer 修复。`npm run check` 499/499 通过（真实库用例按安全门跳过 1 个），独立真实 PostgreSQL 10/10、`db:ping`、`db:migrate`、`db:status`、`/health`、`/ready` 均通过；Mock Agent 的账号→项目→任务→会话→POST run→SSE→transcript 全链路不读取 `.data/works`。浏览器确认初始化失败时错误可见、项目创建/筛选、Agent 会话、输入和发送均禁用，取消入口隐藏且控制台无警告/错误。数据库容器已收紧为 `127.0.0.1:5432`，独立复审无 P0/P1；等待发布与远端 CI 后才能标记完成。 |
| 2026-08-20 | Codex | 对话区鉴权修复已形成独立工作树本地提交 `c9b3e5d` 并通过两次全仓 415/415；因 GitHub 写入权限 403、失效的本机 `gh` 凭据和受限终端网络，远端未产生部分修改。修复已按内容交给数据库配置 / WS-308 任务整合；该任务需保护现有 PostgreSQL 改动、同步最新 `main`、重跑全仓与真实数据库检查、完成页面验收后再非强制快进推送并回传最终 SHA。 |
| 2026-08-20 | Codex | 与数据库配置 / WS-308 任务协调共享 `app.js` 编辑顺序：对话区先基于 `origin/main@659a6be` 提交 AG-103 / AG-204 鉴权与初始化状态回归修复，WS-308 随后在保留 PostgreSQL 配置、迁移和真实集成测试成果的前提下做语义整合。本次对话提交不修改 PostgreSQL、服务端 API、共享 Schema、领域工作流或 WS-308 的所有权与状态；功能提交进入 `main` 后由对话区回传 SHA。对话区本地浏览器验收因用户侧安全策略拒绝而未绕过，WS-308 整合最新 `main` 后负责在真实 PostgreSQL 页面验证认证账号到 Agent run/SSE 完成链路及初始化失败禁用状态；如整合修改 `app.js`，须重新运行 `npm run check`。 |
| 2026-08-18 | Codex | 根据已确认的 Workspace V2 规格建立初始工作区开发清单 |
| 2026-08-18 | Codex | 领取 WS-001，开始盘点现有领域对象与本地存储结构 |
| 2026-08-18 | Codex | 完成 WS-001 迁移盘点初稿，记录兼容字段、废弃字段和本地 v1 数据基线 |
| 2026-08-18 | Codex | WS-001 基线检查通过：typecheck、43 项测试和安全扫描均成功 |
| 2026-08-18 | Codex | 切换当前任务到 WS-002，开始定义 Workspace V2 核心实体 Schema |
| 2026-08-18 | Codex | WS-002 本地验收通过：四个核心实体 Schema、类型导出、边界测试及全仓 47 项测试均成功 |
| 2026-08-18 | Codex | 提交身份统一为 `jiaqi-0625`，并基于最新 `origin/main` 重写：WS-001 `b589528`、WS-002 `5b21d7e` |
| 2026-08-18 | Codex | WS-003 本地验收通过：车型、人物、场景、视觉风格、本地上传、项目资产池和任务快照 Schema 完成，全仓 50 项测试成功 |
| 2026-08-18 | Codex | WS-003 资产契约已基于最新 `origin/main` 重写为 `e664b30`；准备统一推送到 `main` |
| 2026-08-18 | Codex | 经用户授权直接快进集成到 `main`，范围 `58c7a6b..b35ef55`；提交身份统一为 `jiaqi-0625`，全仓检查 53/53 通过 |
| 2026-08-18 | Codex | 切换当前任务到 WS-004，在 `agent/workspace-v2-versioning` 开始阶段版本契约设计 |
| 2026-08-18 | Codex | WS-004 本地验收通过：不可变版本不含更新字段，回退显式记录目标版本，下游失效可定位依赖和传播原因；全仓 58 项测试成功 |
| 2026-08-18 | Codex | 根据用户长期授权，将仓库流程固定为从最新 `origin/main` 开始、检查后直接非强制快进推送 `main`；WS-001～WS-004 按新标准完成 |
| 2026-08-18 | Codex | 开始 WS-005；对话区现有 `StrategyActionProposal` 作为兼容输入，统一契约由共享 Schema 和双方 fixture 冻结 |
| 2026-08-18 | Codex | WS-005 本地验收完成：共享 `TaskContext`/`AgentActionCard` v1 契约、跨包 fixture 和越权字段拒绝测试落地；功能提交 `a8e13b2`，全仓检查 62/62 通过，随本次账本提交进入 `origin/main` |
| 2026-08-18 | Codex | 开始 WS-006；基于 `main@9ed5862` 抽取 API 路由及 Web 工作区/Agent 面板边界，保持现有纵向链路行为不变 |
| 2026-08-18 | Codex | WS-006 本地验收完成：功能提交 `005a41e`，API/Web 独立维护边界和模块资源测试落地；全仓 62/62 通过，浏览器交互无回归，随本次账本提交进入 `origin/main` |
| 2026-08-18 | Codex | 开始 WS-101；基于 `main@b3c3812` 建立 VideoTask 六阶段状态机，旧 Work 状态机仅保留为当前纵向链路兼容 |
| 2026-08-18 | Codex | WS-101 本地验收完成：六阶段合法链路、驳回、跳阶段、错误阶段、Agent 确认和终态转换测试通过；功能提交 `44b1cf0`，全仓 66/66 通过，随本次账本提交进入 `origin/main` |
| 2026-08-18 | Codex | 开始 WS-102；基于 `main@1bba640` 实现服务端人工确认、不可变阶段版本与原子落盘，保持旧 V1 业务链路兼容 |
| 2026-08-18 | Codex | WS-102 本地验收完成：功能提交 `7e8d318`，六阶段人工确认、不可变版本、审计关联、revision 防重和原子保存测试通过；全仓 74/74 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-103；同步到 `origin/main@88f43ee` 后实现历史版本选择、依赖图递归失效和回退聚合原子落盘 |
| 2026-08-19 | Codex | WS-103 本地验收完成：功能提交 `fb3f303`，历史版本选择、直接/传递失效、独立分支保留、失效版本禁用、旧 revision 防重、失败无部分写入和 v1 数据升级测试通过；全仓 85/85 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-104；基于 `origin/main@edadeca` 定义品牌级/车型项目级授权记录和品牌、项目、任务领域权限判定，供后续 SessionScope 与任务 API 统一复用 |
| 2026-08-19 | Codex | WS-104 本地验收完成：功能提交 `80f67f7`，授权 Schema、品牌/项目/任务权限矩阵和确认/回退实际写路径守卫完成；跨租户、跨品牌、撤销授权、他人授权、非负责人和接管边界测试通过，全仓 93/93 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-105；基于 `origin/main@0ea82b0` 实现服务端任务接管审计和按任务串行的乐观锁事务，确保并发接管只有一个请求成功 |
| 2026-08-19 | Codex | WS-105 本地验收完成：功能提交 `2eefee7`，服务端会话接管、不可变负责人转移审计、按任务串行事务、旧 revision 拒绝及 v1/v2 数据升级完成；并发接管仅一个请求成功，全仓 100/100 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-106；基于 `origin/main@8992f1b` 实现账号级高消耗任务运行锁，锁身份取自服务端会话并按租户与账号隔离 |
| 2026-08-19 | Codex | WS-106 本地验收完成：功能提交 `5c48702`，同账号第二条高消耗任务被后端拒绝、不同账号可分别运行；并发获取、权限边界、安全释放、服务重启恢复和 HTTP 409 稳定错误码测试通过，全仓 113/113 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-107；基于 `origin/main@10db85b` 实现服务端账号额度、版本化成本预估、执行前预留及成功结算/失败释放规则 |
| 2026-08-19 | Codex | WS-107 本地验收完成：功能提交 `d391839`，账号额度配置、执行时重新估价、原子预留、实际成本结算和失败释放完成；额度不足在操作执行前由后端拒绝，并发请求不能超额，失败任务零扣费，全仓 132/132 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-201；基于 `origin/main@07efb39` 仅预留公司资产库 Provider 端口和公共 DTO，不实现模拟目录或筛选适配器 |
| 2026-08-19 | Codex | WS-201 本地验收完成：功能提交 `8f40dff`，公司资产查询、分页、批量解析、公共元数据和服务端作用域端口已冻结；未加入模拟资产实现，全仓 134/134 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-202；基于 `origin/main@299f629` 在 `CompanyAssetProvider` 端口内实现可筛选模拟目录，保持 Mock 私有记录不越过公共 DTO 边界 |
| 2026-08-19 | Codex | WS-202 本地验收完成：功能提交 `5aa5d7c`，四类模拟公司资产、授权隔离、组合筛选、游标分页、最新版本目录和历史引用解析完成；全仓 143/143 通过，随本次账本提交进入 `origin/main` |
| 2026-08-19 | Codex | 开始 WS-203；基于 `origin/main@b71e8a8` 实现项目资产池刷新和任务资产快照锁定，项目跟随 Provider 最新版本、已锁定任务保持原版本 |
| 2026-08-19 | Codex | WS-203 本地与远端验收完成：功能提交 `031cee0` 已进入 `origin/main`；项目池从目录 v1 刷新到 v2 后，任务 A 仍保持 v1 快照、任务 B 锁定 v2，任务指针与快照原子落盘，并发旧 revision 仅一项成功；全仓 164/164 通过 |
| 2026-08-19 | Codex | 开始 WS-204；基于 `origin/main@0e0f924` 实现项目级临时资产元数据、格式/尺寸/重复项校验、来源与版权确认，以及仅通过校验后进入项目可用资产池的服务端流程 |
| 2026-08-19 | Codex | WS-204 本地与远端验收完成：功能提交 `ef3145c` 已进入 `origin/main`；合法图片/视频可从 pending 校验为 valid 并进入项目池，版权未确认/重复项待审、技术异常拒绝、过期本地引用阻断任务快照，池保存失败保留有效资产供安全重试；全仓 189/189 通过 |
| 2026-08-19 | Codex | 根据用户决定开始 WS-205 / WS-206：视频生成仅预留目标为 Seedance 2.5 的 Provider 端口，自动剪辑仅预留供应商无关端口；本阶段不实现 Mock、真实调用、轮询执行或自动化剪辑工具适配器 |
| 2026-08-19 | Codex | WS-205 / WS-206 验收完成：Seedance 2.5 视频生成端口提交 `3b663bb`、供应商无关自动剪辑端口提交 `04cbe51` 均已进入 `origin/main`；无适配器、网络调用、凭据或 Agent 工具注册，全仓 193/193 通过 |
| 2026-08-19 | Codex | 开始 WS-301；基于 `origin/main@9288290` 实现服务端开发账号目录、opaque Session 持久化和请求级 `WorkspaceSessionScope` 解析。客户端只提交账号选择或 Session ID，租户、角色和有效授权均由服务端重新装配。 |
| 2026-08-19 | Codex | WS-301 本地验收完成：功能提交 `7c27c81`；开发账号列表、创建/切换/查询/退出 Session、动态授权刷新、token 摘要落盘、跨账号 Agent 会话隔离及生产 fail-closed 边界完成；重放到最新 `origin/main` 后保留 AG-103/AG-501 的会话列表、requestId 幂等和 Last-Event-ID 续传，并兼容 AG-301 的任务素材只读工具，最终安全复审无阻断，全仓 228/228 通过。 |
| 2026-08-19 | Codex | 开始 WS-302；基于 `origin/main@ae4257a` 实现管理中心所需的品牌、车型事实版本、账号授权和额度查询/命令 API，所有管理身份与租户范围继续由 Bearer Session 服务端解析。 |
| 2026-08-19 | Codex | WS-302 本地验收完成：功能提交 `1b0efd2`；管理员已可建立/归档品牌、追加不可变车型事实版本、筛选并关联 Mock 公司资产、授予/撤销品牌和车型项目权限、配置账号额度并查看消费汇总，制作账号可读取本人额度。默认 Session 与管理 API 共用持久化授权源，动态新车型可关联严格绑定的确定性 Mock 车型素材；越权、悬空风格、归档恢复、旧 revision、错版/跨车型资产和不存在账号均稳定拒绝，全仓 237/237 通过。 |
| 2026-08-19 | Codex | 开始 WS-303；基于 `origin/main@2315524` 实现三步创建项目查询与命令 API，车型、推荐资产及替换项、品牌视觉风格、画幅和批次命名全部由服务端授权目录与公司资产 Provider 复核。 |
| 2026-08-19 | Codex | WS-303 本地与远端验收完成：功能提交 `38c0c10` 已进入 `origin/main`；三步查询和原子创建 API 已完成，车型资产服务端锁定、人物/场景可替换、品牌默认风格自动入池、历史引用升级、向导 revision 防陈旧、requestId 幂等并发与撤权串行边界均已覆盖；聚合资产池兼容既有 WS-203 Store 接口，最终安全复审无 P0/P1/P2，全仓 266/266 通过。 |
| 2026-08-19 | Codex | 开始 WS-304；基于 `origin/main@57da79d` 实现视频任务创建、负责人分配、接管和列表 API，复用 WS-105 乐观锁与 WS-303 项目聚合，并确保项目可包含多条独立任务。 |
| 2026-08-19 | Codex | WS-304 功能提交 `40f8750` 已非强制快进推送 `origin/main`；创建/列表/分配/接管 API、服务端身份与动态授权、幂等创建、不可变归属审计及跨 Store 锁定/fencing 已完成；安全复审无 P0/P1/P2，全仓 302/302 通过。下一项为 WS-305。 |
| 2026-08-19 | Codex | 开始 WS-305；基于已同步的 `origin/main@d40ad03` 实现认证会话下的统一 `AgentActionCard` 命令边界，服务端将重新校验任务作用域、当前负责人、revision、工作流状态与真实成本，不信任卡片展示值。 |
| 2026-08-19 | Codex | WS-305 功能提交 `3bf2a01` 已非强制快进推送 `origin/main`；统一 `AgentActionCard` 命令边界、服务端重新鉴权、业务幂等、权威零成本、策略快照/草稿、审批请求和回退 receipt 原子落盘已完成；最终复审无 P0/P1/P2，变基后全仓 332/332 通过。下一项为 WS-306。 |
| 2026-08-19 | Codex | 开始 WS-306；基于 `origin/main@3bf2a01` 实现认证 SessionScope 下的阶段版本与失效查询、显式人工确认和工作区回退 API。所有写入重新校验项目、任务、当前负责人和 revision，并保证确认、版本、回退及失效审计幂等、原子落盘。 |
| 2026-08-19 | Codex | WS-306 功能提交 `9271e1c` 完成；四个阶段生命周期 API、动态授权、跨命令 receipt 幂等、v1–v5→v6 迁移、不可变审计和完整递归失效闭包已通过独立复审，无 P0/P1/P2；全仓 366/366、类型检查和凭据扫描通过。下一项为 WS-307。 |
| 2026-08-19 | Codex | 开始 WS-307；基于 WS-306 功能提交 `9271e1c` 迁移现有作品到“示例品牌 → 车型项目 → 视频任务”，保留策略、状态、版本、锁定和审批记录。 |
| 2026-08-19 | Codex | WS-307 功能提交变基为 `064f74a`：迁移 CLI、备份/租约/断点恢复、V2 Agent 上下文与车型快照、V1 写禁用和 Web 存储键迁移均已完成；整合 WS-402、AG-404 后复核无 P0/P1/P2，全仓 415/415 通过。只读预演确认 87 个 Work、35 个 Session、124 个目标写入且 manifest 前后均为空；未执行真实 `apply`。任务转为阻塞，等待业务配置冻结、停服窗口和最终 plan hash 审批。 |
| 2026-08-19 | Codex | WS-401 功能提交 `83cbbad` 完成并进入用户评审；按萤火虫平台嵌入要求移除最左侧分类区，保留顶部品牌与账号切换，1280/1920 桌面布局、1279 小屏提示、明暗主题、多品牌上下文隔离及零控制台错误验收通过；全仓检查 338/338 通过。未开始 WS-402。 |
| 2026-08-19 | Codex | 用户确认 WS-401 基本实现并同意进入下一项，WS-401 标记为已完成。 |
| 2026-08-19 | Codex | WS-402 功能提交 `1f8e035` 完成并进入用户评审；认证项目库、“我的进行中任务”、中文搜索、品牌/车型/状态筛选和最近活动排序已实现。独立代码、安全与视觉复核问题全部闭环；全仓 371/371、类型检查和凭据扫描通过，桌面宽度、明暗主题、账号切换、错误恢复及零控制台错误验收通过。未开始 WS-403。 |
| 2026-08-20 | Codex | 用户确认进入 WS-403，WS-402 标记为已完成。WS-403 功能提交 `8263778` 已进入 `origin/main` 并等待用户评审；按用户复核意见将三步向导收敛为单页，仅填写获权品牌/车型、10/15/30 秒时长和创意特效型/情景演绎/常规口播类型。车型推荐资产与品牌风格由服务端自动装配，首条 Agent 任务自动建立并携带时长和类型；项目/任务分别使用稳定 requestId，任务失败可安全重试。后续由 Agent 按描述选取资产，分镜逐镜展示引用素材并允许人工调整，人工结果优先。全仓 420/420、类型检查和凭据扫描通过；1280/1920、1279 小屏提示、明暗主题、管理员限制、失败重试、创建后项目库刷新及零控制台错误验收通过。未开始 WS-404。 |
| 2026-08-20 | Codex | 用户确认精确选材必须位于策略和脚本之后，以充分利用资产内置描述词提高匹配准确度；已记录为[重要产品决策](../decisions/2026-08-20-asset-matching-after-script.md)，新增 WS-108 调整领域工作流顺序，WS-406/WS-407 等待新顺序落地。WS-403 创建页和首条任务逻辑不受影响，仍保持待评审，未开始 WS-404。 |
| 2026-08-20 | Codex | 用户确认开始 WS-404，WS-403 标记为已完成。基于 `origin/main@d794220` 实现 V2 三栏项目工作区框架；仅接入项目库到工作区的结构导航，任务 URL、状态同步和上下文事件留待 WS-405。 |
| 2026-08-20 | Codex | WS-404 功能提交 `ca3a705` 完成并进入用户评审。项目库和“我的任务”现可进入三栏工作区：左栏承载项目与视频任务，中栏提供策划/资产/分镜/制作/交付模块框架，右栏预留任务绑定的萤火虫助手；未加入平台最左侧分类区。全仓 424/424、TypeScript 和凭据扫描通过；1280/1920 桌面宽度、1279 小屏提示、明暗主题及零控制台错误验收通过。任务 URL、状态同步和上下文事件留待 WS-405，尚未开始 WS-405。 |
| 2026-08-20 | Codex | 用户确认 WS-404 验收通过并进入 WS-405，WS-404 标记为已完成。WS-405 从 `origin/main@7fc45d6` 开始，补齐项目概览、任务负责人和状态展示，以及任务切换的 URL、界面状态和上下文事件同步。 |
| 2026-08-20 | Codex | WS-405 功能提交 `82613d0` 完成并进入用户评审。项目入口默认展示项目事实与任务概况，中央任务清单和左栏同步展示阶段、状态、负责人归属及更新时间；任务切换同步 URL、浏览器历史、本地任务选择、右侧助手上下文和 `firefly:workspace-task-context-change` 事件，事件不携带账号身份。重放到最新 `origin/main` 后全仓 502/502、TypeScript 和凭据扫描通过，1 个真实 PostgreSQL 用例按安全门跳过；1280/1920、1279 小屏提示、明暗主题、当前/其他负责人、前进后退恢复、零横向溢出和零控制台错误验收通过。未开始 WS-406。 |
| 2026-08-20 | Codex | WS-406 / WS-108 / AG-307 重要功能提交 `b98564e` 完成并进入用户评审：选材调整到策略和脚本确认之后，Agent 获得确认版本与素材描述词；页面支持车型锁定、人物/场景人工替换和项目临时图片上传，人工结果优先。重放最新 `origin/main` 后全仓 517/517、类型检查和凭据扫描通过，1 个真实 PostgreSQL 用例按安全门跳过；1920px、1279px、明暗主题、上传失败反馈、零横向溢出和零控制台错误验收通过。未开始 WS-407。 |
| 2026-08-20 | Codex | 用户确认进入 WS-407。功能提交 `ba84084` 完成并进入用户评审：策略、脚本、逐镜分镜、模拟预览和交付模块已接入真实版本查询、人工确认和回退 API；分镜展示车型/人物/场景素材并支持人物、场景人工调整，车型保持锁定；回退明确提示所有下游失效阶段。前端不伪造未持久化产物，真实视频与剪映 Provider 未接入时明确显示模拟或等待状态。全仓 521/521、类型检查和凭据扫描通过，1 个真实 PostgreSQL 用例按安全门跳过；1920/1280、1279 小屏提示、明暗主题、零横向溢出和零控制台错误验收通过。未开始 WS-408。 |
| 2026-08-20 | Codex | 用户确认 WS-407 效果并进入 WS-408，WS-407 标记为已完成。WS-408 功能提交 `d8a174e` 完成并进入用户评审：制作与交付模块显示生成前费用、本人额度和账号运行槽；前端严格校验额度范围与恒等式，运行状态区分可用、本任务和其他任务占用，拒绝提示与后端稳定错误代码一致。真实定价与生成 Provider 未接入，不展示虚构报价。全仓 548/548、类型检查和凭据扫描通过，1 个真实 PostgreSQL 用例按安全门跳过；1920/1280、1279 小屏提示、明暗主题、全部额度与运行槽状态、零横向溢出和零控制台错误验收通过。未开始 WS-409。 |

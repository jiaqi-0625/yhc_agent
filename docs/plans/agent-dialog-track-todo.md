# 对话区开发线待办

> 负责人：对话区负责人
> 推荐分支：`agent/task-agent-panel`
> 当前状态：待开始
> 当前任务：无
> 最近更新：2026-08-18
> 关联 PR：无

本文是对话区开发线的实时任务账本。产品边界以 [`docs/workspace-v2-product-spec.md`](../workspace-v2-product-spec.md) 为准；本文只记录任务级 Agent、会话、流式交互和操作卡片的实施状态。

## 状态规则

| 状态 | 含义 |
|---|---|
| `待开始` | 依赖未满足或尚未领取 |
| `进行中` | 负责人当前正在处理；同一负责人原则上只保留一项 |
| `阻塞` | 无法继续，必须填写阻塞原因和解除条件 |
| `待评审` | 已提交 PR，等待检查或评审 |
| `已完成` | 已合并到 `main`，验证结果已记录 |

## 实时维护要求

1. 开始工作前更新页首的“当前任务、当前状态、最近更新、关联 PR”。
2. 将领取任务的状态改为 `进行中`，并记录实际分支。
3. 每次推送后记录最新提交或 PR；Agent 行为变化不能只记录在提示词中。
4. 发生阻塞时当天改为 `阻塞`，写清所缺的 Schema、API、产品决定或测试环境。
5. 只有 PR 合并、会话/权限测试通过并完成浏览器联调后才能标记 `已完成`。
6. 对 `TaskContext`、`AgentActionCard` 或可执行工具提出变更时，先与工作区负责人评审，再修改共享契约。
7. 每个工作日结束前刷新一次本页状态，并在更新记录中写明对工作区的影响。

## 代码所有权

对话区负责人主要维护：

- `packages/agent` 中任务级 Agent、会话、系统提示词、流式事件和工具装配；
- Agent 专用的共享消息、计划和操作卡片 Schema；
- `apps/api` 中 Agent 会话、恢复、取消和流式端点；
- `apps/web` 中右侧 Agent 面板、时间线、上下文提示和操作卡片交互；
- Agent 会话隔离、脱敏、审计和工具边界测试。

共享热点：

- `packages/schemas/src/index.ts`：不单方面改变工作区领域对象；通过 WS-005 共同冻结契约。
- `packages/domain`：Agent 不直接添加状态转换；需要新命令时由工作区负责人实现并评审。
- `apps/api/src/server.ts`：优先抽取 Agent 路由，避免与工作区 API 同文件冲突。
- `apps/web/public/app.js`、`app.css`、`index.html`：优先把 Agent 面板迁移到独立模块和样式边界。
- 生产 Agent 不注册 shell、文件系统、SQL、任意 HTTP、浏览器或人工批准工具。

## 里程碑 0：共享契约与模块边界

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-001 | 盘点现有会话、流式时间线和作品绑定实现 | 无 | 待开始 | 记录可复用能力、耦合点和需要迁移的事件 |
| AG-002 | 向工作区提出 `TaskContext` 最小字段需求 | AG-001 | 待开始 | 不包含可由模型伪造的身份或权限字段 |
| AG-003 | 与工作区共同冻结 `TaskContext` 和 `AgentActionCard` Schema | AG-002, WS-002 | 待开始 | 双方 fixture 和 Schema 测试一致 |
| AG-004 | 将 Agent API 和前端面板从单体文件拆为独立模块 | AG-003, WS-006 | 待开始 | 与工作区开发线不再长期修改同一区块 |
| AG-005 | 建立对话区 Mock `TaskContext` fixture | AG-003 | 待开始 | 工作区 API 未完成时可独立开发和测试 |

## 里程碑 1：任务级会话与上下文

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-101 | 将会话绑定从单条旧 `workId` 迁移为 `videoTaskId` | AG-003 | 待开始 | 会话记录包含租户、项目和任务作用域 |
| AG-102 | 实现任务切换时的上下文加载与显式提示 | AG-101 | 待开始 | UI 清楚显示当前品牌、车型、项目和任务 |
| AG-103 | 实现跨任务、跨项目和跨账号会话隔离 | AG-101 | 待开始 | 越权恢复会话被后端拒绝并有测试 |
| AG-104 | 实现会话新建、恢复、取消和重置 | AG-101 | 待开始 | 不改变业务状态，取消不产生重复副作用 |
| AG-105 | 为任务上下文生成精简只读摘要 | AG-102 | 待开始 | 包含快照版本和当前阶段，不泄漏无权数据 |

## 里程碑 2：流式事件与 Agent 面板

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-201 | 固化流式事件 Schema | AG-004 | 待开始 | 覆盖消息、思考摘要、工具状态、计划、错误和完成 |
| AG-202 | 保留并迁移现有 Agent 时间线能力 | AG-201 | 待开始 | 当前 `main` 的流式时间线无功能回退 |
| AG-203 | 实现任务级消息列表和会话选择 | AG-104, AG-202 | 待开始 | 切换任务不混合消息，恢复顺序稳定 |
| AG-204 | 实现连接中、取消、重试、断线恢复和错误状态 | AG-202 | 待开始 | 重连不重复展示事件或执行工具 |
| AG-205 | 实现 Agent 面板宽度调整、折叠和键盘交互 | AG-004 | 待开始 | 1280–1920px 可用且不遮挡主工作区 |
| AG-206 | 实现上下文条、额度提示和任务负责人提示 | AG-102, WS-107 | 待开始 | 前端展示与服务端 SessionScope 一致 |

## 里程碑 3：领域工具与建议生成

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-301 | 按当前任务动态装配领域只读工具 | AG-105, WS-203 | 待开始 | 工具只能访问任务快照和允许的公司资产 |
| AG-302 | 实现营销策略建议能力 | AG-301, WS-101 | 待开始 | 输出通过版本化 Schema，不直接推进状态 |
| AG-303 | 实现资产推荐能力 | AG-301, WS-201 | 待开始 | 车型资产不可替换，人物/场景推荐带版本引用 |
| AG-304 | 实现脚本、分镜和交付阶段建议 | AG-302, AG-303 | 待开始 | 只在允许阶段生成建议，遵守已确认上游产物 |
| AG-305 | 实现事实、禁用表达和资产来源风险提示 | AG-301 | 待开始 | 风险可定位到事实或资产，不自动忽略 |
| AG-306 | 限制生产 Agent 工具白名单和结果脱敏 | AG-301 | 待开始 | 未知/通用工具默认拒绝，敏感字段不进入消息 |

## 里程碑 4：操作卡片

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-401 | 生成结构化 `AgentActionCard` | AG-003, AG-302 | 待开始 | 包含 taskId、action、expectedRevision、摘要和成本 |
| AG-402 | 实现操作卡片 UI 与明确的用户执行按钮 | AG-401, AG-203 | 待开始 | Agent 不能自动点击或默认确认 |
| AG-403 | 接入工作区统一命令 API | AG-402, WS-305 | 待开始 | 权限、状态、revision 和预算由后端重新校验 |
| AG-404 | 展示卡片过期、越权、额度不足和状态冲突 | AG-403 | 待开始 | 错误可恢复，不重复提交命令 |
| AG-405 | 将成功执行结果回写为时间线事件 | AG-403, AG-202 | 待开始 | 会话展示结果，但业务状态仍来自工作区 API |

## 里程碑 5：测试与集成

| ID | 待办 | 依赖 | 状态 | 完成标准 |
|---|---|---|---|---|
| AG-501 | 增加任务/项目/账号会话隔离测试 | AG-103 | 待开始 | 覆盖恢复、切换和越权路径 |
| AG-502 | 增加流式事件顺序、取消和重连测试 | AG-204 | 待开始 | 不丢事件、不重复事件、不重复执行工具 |
| AG-503 | 增加工具策略、脱敏和操作卡片测试 | AG-306, AG-404 | 待开始 | Agent 无法批准、越权或绕过额度 |
| AG-504 | 与工作区联调任务切换和上下文同步 | AG-203, WS-404 | 待开始 | 左侧任务、中间工作区和 Agent 上下文一致 |
| AG-505 | 与工作区联调操作卡片完整链路 | AG-405, WS-305 | 待开始 | 从建议到人工点击、后端执行和结果事件闭环 |
| AG-506 | 完成桌面端浏览器验收和全仓检查 | AG-505, WS-503 | 待开始 | `npm run check` 通过，不产生真实模型费用 |
| AG-507 | 更新 Agent 架构、事件协议和调试说明 | AG-506 | 待开始 | 文档与最终实现一致，移除过时描述 |

## 当前阻塞

无。

## 工作区契约通知

- 2026-08-18：WS-002 已在 `agent/workspace-v2-foundation` 提交为 `4302ac8`，尚未提交 PR 或合并。`VideoTask` 的任务上下文字段包括 `tenantId`、`batchProjectId`、`ownerAccountId`、`currentStage`、`revision` 和可选 `vehicleSnapshotId`；AG-002/AG-003 不应创建平行类型，最终冻结仍需双方评审。

## 更新记录

| 日期 | 更新人 | 说明 |
|---|---|---|
| 2026-08-18 | Codex | 根据已确认的 Workspace V2 规格建立初始对话区开发清单 |
| 2026-08-18 | Codex | 收到 WS-002 共享核心实体契约通知；AG-003 继续依赖其评审和合并结果 |
